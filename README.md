# Ger Translator

Ger Translator is a premium web application for extracting structured German vocabulary from uploaded documents and turning it into a CEFR-filtered learning pack with downloadable PDF and CSV exports.

## Highlights

- Uploads PDF, DOCX, and PPTX files
- Extracts text from multi-page documents and slide decks
- Detects whether a document is sufficiently German
- Uses a German spaCy pipeline plus a curated CEFR lexicon to structure vocabulary
- Groups output into nouns, verbs, adjectives, adverbs, prepositions, and phrases
- Adds English translations from an offline glossary, with optional DeepL enrichment
- Generates a polished A4 PDF and a CSV companion export
- Tracks recent processing jobs with progress states and downloadable history

## Tech Stack

- Backend: FastAPI
- Frontend: Server-rendered HTML, CSS, and vanilla JavaScript
- NLP: `spaCy` with `de_core_news_sm`, plus curated CEFR vocabulary data and fallback heuristics
- File parsing:
- PDF: `PyPDF2`
- DOCX: `python-docx`
- PPTX: `python-pptx`
- Language detection: `langdetect`
- Export generation: `reportlab` and Python CSV tooling

## Project Structure

```text
app/
  api/
  core/
  models/
  repositories/
  services/
scripts/
data/
static/
templates/
tests/
docs/
```

## Setup

1. Create a virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
```

2. Install dependencies:

```bash
pip install -r requirements.txt
```

3. Optional: set environment variables:

```bash
cp .env.example .env
```

4. Start the server:

```bash
uvicorn app.main:app --reload
```

5. Open [http://127.0.0.1:8000](http://127.0.0.1:8000)

## Example Flow

1. Upload a `.pdf`, `.docx`, or `.pptx` file
2. Choose a CEFR level from `A1` to `C2`
3. Click `Generate Vocabulary`
4. Wait for processing to complete
5. Preview the grouped vocabulary
6. Download the PDF or CSV export

## API Notes

- FastAPI interactive docs are available at `/docs`
- A concise endpoint summary is available in [docs/api.md](/Users/adi/Documents/Projects/Ger_Translator/docs/api.md)

## CEFR Data Notes

- The main dataset in [data/cefr_vocabulary.csv](/Users/adi/Documents/Projects/Ger_Translator/data/cefr_vocabulary.csv) now contains 32,000 rows
- The original curated starter set is preserved in [data/cefr_vocabulary_seed.csv](/Users/adi/Documents/Projects/Ger_Translator/data/cefr_vocabulary_seed.csv)
- The large CSV is generated by [scripts/build_cefr_vocabulary.py](/Users/adi/Documents/Projects/Ger_Translator/scripts/build_cefr_vocabulary.py) from:
- the curated seed rows
- German frequency data
- the official FreeDict German-English dictionary
- The data is organized by CEFR level, category, lemma, display term, translation, article, and optional aliases

## NLP Notes

- Primary analysis uses `spaCy` with the `de_core_news_sm` model for tokenization, POS tagging, and lemmatization
- Phrase extraction and CEFR assignment are reinforced by the bundled CEFR lexicon
- If spaCy is unavailable in a target environment, the app falls back to lexicon and regex-based extraction

## Translation Notes

- Offline mode uses the bundled glossary derived from the CEFR dataset
- If `DEEPL_API_KEY` is set, unresolved entries can be sent to DeepL for broader English translation coverage

## Running Tests

```bash
python -m unittest discover -s tests
```

## Deployment Readiness

- Async job handling is implemented through FastAPI background tasks
- Job state is persisted in SQLite
- Static assets are served directly by FastAPI
- The architecture cleanly separates parsing, NLP, translation, export, and repository responsibilities

## Future Enhancements

- Redis or Celery-backed queueing for larger workloads
- Persistent upload history with authentication
- Original-text highlighting for extracted vocabulary
- Custom filters such as verbs-only or nouns-only exports
- Swappable translation providers and larger CEFR datasets
